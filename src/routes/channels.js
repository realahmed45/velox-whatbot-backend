// Multi-channel (WhatsApp/TikTok via Zernio) routes — filled in by the
// channel module. Stub keeps the server bootable while the module lands.
const express = require("express");
const router = express.Router();
module.exports = router;
