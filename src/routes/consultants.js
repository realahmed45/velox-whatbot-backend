// Consultant (marketer) routes — filled in by the consultant module. Stub
// keeps the server bootable while the module lands.
const express = require("express");
const router = express.Router();
module.exports = router;
